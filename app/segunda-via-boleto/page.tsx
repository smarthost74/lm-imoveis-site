import { redirect } from "next/navigation";

/** Destaque na navegação — costuma ser a página de serviço mais acessada (ver briefing seção 9). */
export default function SegundaViaBoletoPage() {
  redirect("https://fgaconsult.superlogica.net/clients/areadocliente");
}
